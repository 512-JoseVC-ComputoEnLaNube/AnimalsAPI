info:
  name: AnimalsPatch
  type: http
  seq: 3

http:
  method: PATCH
  url: http://localhost:3000/animals/2
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
